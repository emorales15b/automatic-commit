#!/usr/bin/env bash

cd $(dirname ${BASH_SOURCE[0]})

if [[ -n $(git status -s) ]]; then
    echo "Changes found. doing changes"
    yo = 'test'
    git add -A && git commit -m '${yo}text'
else
    echo "No changes found."
fi
